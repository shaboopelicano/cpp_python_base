{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "%cd%/tools/iniciar-cl.cmd && %cd%/tools/buildar.cmd && main.exe && exit",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "echo": true,
                // "reveal": "never",
                "focus": true,
                "panel": "new"
            }            
        }
    ]
}